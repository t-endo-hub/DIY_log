$(".recipes_form_<%= @post.id %>").html("<%= j(render 'recipes/recipes_form',{ recipes: @recipes, post: @post} ) %>");
$(".recipes_form").val("");
$("#recipe-prev").attr('src', '<%= image_url("no_pic.png") %>')
